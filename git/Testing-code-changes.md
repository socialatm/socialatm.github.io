When testing changes to the installer:

* Delete `.htconfig.php` and `php.out` files.  
* Drop all database tables but leave your empty database in place.
* Go to your site in the browser and the installer will start.
